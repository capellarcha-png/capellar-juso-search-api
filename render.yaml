services:
  - type: web
    name: juso-search-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JUSO_API_KEY
        sync: false
